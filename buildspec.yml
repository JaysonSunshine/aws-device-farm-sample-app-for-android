version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
artifacts:
  files:
     - '$CODEBUILD_SRC_DIR/android/app/build/outputs/**/*.apk'
  discard-paths: yes